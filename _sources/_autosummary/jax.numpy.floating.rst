jax.numpy.floating
==================

.. currentmodule:: jax.numpy

.. autoclass:: floating

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~floating.__init__
      ~floating.all
      ~floating.any
      ~floating.argmax
      ~floating.argmin
      ~floating.argsort
      ~floating.astype
      ~floating.byteswap
      ~floating.choose
      ~floating.clip
      ~floating.compress
      ~floating.conj
      ~floating.conjugate
      ~floating.copy
      ~floating.cumprod
      ~floating.cumsum
      ~floating.diagonal
      ~floating.dump
      ~floating.dumps
      ~floating.fill
      ~floating.flatten
      ~floating.getfield
      ~floating.item
      ~floating.max
      ~floating.mean
      ~floating.min
      ~floating.nonzero
      ~floating.prod
      ~floating.put
      ~floating.ravel
      ~floating.repeat
      ~floating.reshape
      ~floating.resize
      ~floating.round
      ~floating.searchsorted
      ~floating.setfield
      ~floating.setflags
      ~floating.sort
      ~floating.squeeze
      ~floating.std
      ~floating.sum
      ~floating.swapaxes
      ~floating.take
      ~floating.to_device
      ~floating.tobytes
      ~floating.tofile
      ~floating.tolist
      ~floating.tostring
      ~floating.trace
      ~floating.transpose
      ~floating.var
      ~floating.view
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~floating.T
      ~floating.base
      ~floating.data
      ~floating.device
      ~floating.dtype
      ~floating.flags
      ~floating.flat
      ~floating.imag
      ~floating.itemset
      ~floating.itemsize
      ~floating.nbytes
      ~floating.ndim
      ~floating.newbyteorder
      ~floating.ptp
      ~floating.real
      ~floating.shape
      ~floating.size
      ~floating.strides
   
   